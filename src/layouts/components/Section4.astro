---
import img1 from "/public/images/texture_bg/bg_leaft.png";

import {
  getLangFromUrl,
  useTranslations,
  useTranslatedPath,
} from "../../i18n/utils";
import HeaderLink from "../components/HeaderLink.astro";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const translatePath = useTranslatedPath(lang);

const link = {
  ref: "01",
  href1: translatePath("/frutucuman"),
  href2: translatePath("/quality"),
};
---

<section
  class="flex flex-col lg:grid lg:grid-cols-3 w-full font-primary px-0 lg:p-24 lg:pb-0 h-full">
  <img
    src={img1.src}
    alt=""
    class="w-full h-[250px] lg:h-[75vh] object-cover lg:col-span-2"
  />
  <div
    class="col-span-1 flex flex-col items-start justify-center text-start w-full lg:w-[1/3] px-8 bg-black h-full lg:h-full lg:px-12 py-12 lg:py-0">
    <HeaderLink href={link.href1} locale={lang}>
      <h4
        class="text-2xl lg:text-3xl mb-2 lg:mb-4 text-secondary transition-all duration-300 ease-in-out hover:underline font-normal">
        De Frutucumán nace Inducítrica
      </h4>
    </HeaderLink>
    <p class="text-lg lg:text-xl text-white mb-4 lg:mb-16">
      Con la fuerza de la tradición y el impulso de la innovación, somos líderes
      en la industria de derivados del limón.
    </p>
    <HeaderLink href={link.href2} locale={lang}>
      <h4
        class="text-2xl lg:text-3xl lg:mb-4 text-secondary transition-all duration-300 ease-in-out hover:underline font-normal">
        La calidad empieza con el limón
      </h4>
    </HeaderLink>
  </div>
</section>
