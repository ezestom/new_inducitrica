---
import { humanize, markdownify, slugify } from "@/lib/utils/textConverter";

const { page_data } = Astro.props || {};
const { title, index_title, page_title, slug, content } = page_data || {};
---

<div
  class="text-center rounded-t-3xl py-12 shadow lg:min-h-[50vh]"
  id="hero-sections">
  <!-- <ul
    class="breadcrumb inline-flex h-8 items-center justify-center space-x-2 rounded-3xl bg-theme-light px-4 py-2">
    <li class="leading-none text-dark flex items-center justify-center">
      <a href="/" class="inline-flex items-center text-primary">
        <svg
          class="mr-1.5"
          width="15"
          height="15"
          viewBox="0 0 16 16"
          fill="none"
          xmlns="http://www.w3.org/2000/svg">
          <path
            d="M13.1769 15.0588H10.3533V9.41178H5.64744V15.0588H2.82391V6.58825H1.88274V16H14.118V6.58825H13.1769V15.0588ZM6.58862 15.0588V10.353H9.41215V15.0588H6.58862ZM15.8084 6.09225L15.2512 6.85178L8.00038 1.52472L0.749559 6.8499L0.192383 6.09131L8.00038 0.357666L15.8084 6.09225Z"
            fill="black"></path>
        </svg>
        <span class="text-sm leading-none">Inicio</span>
      </a>
    </li>
    <li class="leading-none text-dark">
      {
        index_title ? (
          <span class="text-sm leading-none">
            /{" "}
            <a href={`/${slugify(index_title)}`} class="text-primary">
              {index_title}
            </a>{" "}
            / {page_title ? page_title : humanize(slug.replace("page /", ""))}
          </span>
        ) : (
          <span class="text-sm leading-none">/ {title}</span>
        )
      }
    </li>
  </ul> -->
  <div class="w-full text-balance lg:px-24 lg:py-6">
    <h6
      class="font-black text-[2rem] pt-4 lg:pt-0 lg:!text-[4vw] text-white motion-delay-[5ms] motion-scale-in-[0.8] motion-translate-x-in-[0%] motion-translate-y-in-[-42%] motion-duration-[500ms]"
      set:html={markdownify(
        page_data?.page_title ? page_data?.page_title : page_data?.title
      )}
    />
  </div>

  <!-- {
    page_data.buttons && (
    <div class="mt-11 justify-center sm:flex">
      {page_data.buttons.map(
          (button: any) =>
          button?.enable && (
          <a
          class={`btn m-3 block sm:inline-block ${
            button?.outline ? "btn-outline-primary" : "btn-primary"
            } `}
            href={button?.link}
            >
            {button?.label}
            </a>
            )
            )}
            </div>
            )
            } -->
</div>

<div
  class="container mx-auto py-12 rounded-b-3xl bg-white p-5 shadow-lg lg:p-10">
  <p
    class="text-start text-sm lg:text-xl mx-auto text-balance max-w-[75ch]"
    set:html={markdownify(content)}
  />
</div>

<style>
  #hero-sections {
    background: linear-gradient(rgba(0, 0, 0, 0.1), rgba(0, 0, 0, 0.1)),
      url("/images/inducitrica/quinta2.jpg");
    width: 100%;
    background-size: cover;
    background-position: center center;
    background-attachment: fixed;
    z-index: 10;
  }
  @media screen and (max-width: 768px) {
    #hero-sections {
      background-attachment: scroll;
    }
    
  }
</style>
